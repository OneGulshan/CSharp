using System;
using System.Collections.Generic;
using System.Linq;

namespace OOPS
{
    class Student
    {
        //public static void Main()
        //{
        //    var dateList = new List<DateTime>();
        //    dateList.Add(DateTime.ParseExact("25/09/2022", "dd/MM/yyyy", null));
        //    dateList.Add(DateTime.ParseExact("20/10/2022", "dd/MM/yyyy", null));
        //    dateList.Add(DateTime.ParseExact("20/10/2022", "dd/MM/yyyy", null));
        //    dateList.Add(DateTime.ParseExact("21/06/2022", "dd/MM/yyyy", null));
        //    var tagList = new List<decimal?>() { null, -1, 1, 2 };

        //    var dateL = dateList.Zip(tagList, (a, b) => new { a, b }).ToList();

        //    var removeduplicate = dateL.Where(x => x.b != null).GroupBy(x => new { x.a }).Select(x => x.Last()).ToList();

        //    foreach (var item in removeduplicate)
        //    {
        //        Console.WriteLine("{0} {1}", item.b, item.a);
        //    }

        //    Console.WriteLine();
        //    var removeduplicates = dateL.Where(x => x.b != null).Distinct().Select(x => new { x.a, x.b }).ToList();

        //    foreach (var item in removeduplicates)
        //    {
        //        Console.WriteLine("{0} {1}", item.b, item.a);
        //    }
        //    Console.ReadLine();
        //}
    }
}
